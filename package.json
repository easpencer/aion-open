{
  "name": "aion-open",
  "version": "1.0.0",
  "private": true,
  "description": "Aion Open — Your health data, your tools, your rules",
  "scripts": {
    "start": "npx serve public -s -l tcp://0.0.0.0:${PORT:-3000}",
    "dev": "npx serve public -s -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
